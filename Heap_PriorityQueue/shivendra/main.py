'''
to convert into maxHeap:
    maxHeap = [- num for num in <list>] : [] NOT {} as {}=set, and []= list
to covert the answer back to positive value:
    abs(ans) : DONOT forget to make it back positive using abs
'''